---
title: Errors and Attempts
description: Compact attempt records for the books section.
date: "2026-03-15"
updated: "2026-03-30"
order: 4
---

# Errors and Attempts

| Id | Status | Context | Problem | Response |
| --- | --- | --- | --- | --- |
| `books-reader-01-attempt-01` | `resolved` | early home-page reader pass | reader felt like a preview instead of the real surface | treat the homepage reader as the primary reading surface and keep iterating there |
| `books-reader-01-attempt-02` | `resolved` | first books planning-doc pass | planning pages were too generic and not close to the XML structure | split planning into explicit page types with stable record fields |
| `books-reader-01-attempt-03` | `resolved` | homepage embedded EPUB load | embedded reader auto-load threw `META-INF/container.xml` / missing-asset errors and the CTA only scrolled | defer mount to an explicit load action and use the direct EPUB URL in `EpubViewer` instead of a blob wrapper |
| `books-reader-01-attempt-04` | `resolved` | EPUB packaging for reader images | generated EPUB pages referenced `images/<uuid>.null` or source paths for illustrations that were not actually packaged | rewrite available book images to local asset paths during EPUB generation and strip source image tags when the file is missing |
| `books-reader-01-attempt-05` | `resolved` | upcoming book without manuscript pages | `build:books` logged a failed EPUB build for a book intentionally shown as `coming soon` | detect missing markdown sources early and skip EPUB packaging without treating it as a failure |
| `books-reader-01-attempt-06` | `resolved` | front-page reader still using packaged EPUB URL | browser still requested `/META-INF/container.xml` after the packaging fix because the runtime path could still fall back to directory-style EPUB resolution | fetch the `.epub` archive as an `ArrayBuffer` in `EpubViewer` and hand those bytes directly to `react-reader` |
| `books-reader-01-attempt-07` | `resolved` | homepage embed still diverged from the stable reader route | dedicated reader (`/apps/reader*book=...`; legacy `/books/<slug>/read`) rendered correctly while the front-page embed still failed to render consistently | stop treating the homepage as a second reader runtime and route readers from the front page into the reader app |
| `books-reader-01-attempt-08` | `resolved` | dedicated reader header only exposed a back link and title | switching books from the active reading session required backing out to the index | move book switching into the read-page header and keep unavailable books visibly disabled |
| `books-reader-01-attempt-09` | `resolved` | dedicated reader still felt like a narrow util shell on wide screens | widescreen reading did not use the available space and the TOC treatment looked flat | switch the reader to paginated spread-friendly rendering and restyle the TOC chrome and document typography |
| `books-reader-01-attempt-10` | `resolved` | dedicated reader spread sat too close to the TOC toggle chrome | the first page felt cramped against the left-side controls | move the reader stage farther right in reader mode so the spread clears the toggle area |
| `books-reader-01-attempt-11` | `resolved` | emitted EPUB pages did not fit the dedicated reader spread | lead images could jump to the next column, opening text disappeared from the first spread, and there were no visible bottom folios | emit each source page with a custom page wrapper, extracted lead image block, custom EPUB CSS, and footer folio metadata |
| `books-reader-01-attempt-12` | `resolved` | first bottom-nav pass on the dedicated reader | the footer controls were too low-contrast and too low in the frame, while the old edge chevrons still remained active | raise and compress the footer strip, strengthen its contrast slightly, and hide the legacy side arrows in reader mode |
| `books-reader-01-attempt-13` | `resolved` | manuscript structure still exposed raw page filenames | the EPUB treated every page file as a chapter, so TOC entries and visible titles were driven by `page-<n>` filenames instead of book structure | formalize chapter metadata and page conventions, then rebuild the EPUB around chapter opener entries plus content pages excluded from TOC |
| `books-reader-01-attempt-14` | `resolved` | first structured EPUB build only showed chapter openers in the reader | `epub-gen` excludes `excludeFromToc` entries from the spine as well as the TOC, so story pages disappeared from actual reading order | override the OPF template so story pages stay in the spine while remaining absent from the TOC |
| `books-reader-01-attempt-15` | `resolved` | first full structured EPUB still left blank companion pages beside some lead images | lead images were often still embedded at the start of the first paragraph, so the builder missed them as standalone figures and never switched into the split spread layout | lift leading inline images out of the opening paragraph during EPUB generation, then emit those pages with a split content layout so images and opening text can share the same spread when space allows |
| `books-reader-01-attempt-16` | `resolved` | split spread treatment made the book feel worse once the source pages were viewed as authored paperback pages | the reader showed two pages at a time and the split image/text layout fought the expectation that each manuscript page owns one illustration slot | return the dedicated reader to single-page pagination and emit a fixed illustration frame for every page, using placeholder art when a page has no image |
| `books-reader-01-attempt-17` | `resolved` | single-page mode and the built-in TOC still produced bad reading artifacts | hidden TOC items were visibly bleeding through the left side, while oversized image slots and larger type caused blank or near-empty spread locations | replace the built-in TOC with a custom collapsed panel, restore spreads only on wide screens, and shrink the emitted page shell so the image sits at the top and text starts immediately beneath it |
| `books-reader-01-attempt-18` | `resolved` | some spread locations still showed a portrait lead image by itself with the prose delayed until the following location | stacked figure-then-body layout was still too rigid for taller lead images, even after the page shell was compacted | allow lead-image story pages to switch into an image-left text-right layout on wider spreads so the opening text starts immediately beside the figure |
| `books-reader-01-attempt-19` | `resolved` | the temporary lead-image split made illustrated pages look inconsistent and gave real images different shapes than the placeholder frame | the split layout and `contain` fit preserved source proportions, but the user-facing result was a mix of portrait, wide, and placeholder shapes instead of one stable page rhythm | remove the split rule and force all illustrated pages into the same fixed image frame shape as the placeholder, using crop-first rendering for now |
| `books-reader-03-attempt-01` | `resolved` | first `/apps/reader` shell shipped as a local-file-first utility surface | the route technically worked, but it hid built-in books behind a file picker and had no shelf metadata or progress signals, so it did not feel like a real reading library | extend the generated manifest with author/status/optional cover metadata, persist a lightweight progress value from `EpubViewer`, and redesign the default shell around a built-in shelf with Empty-cover fallback cards and a secondary local-import utility card |
| `books-reader-03-attempt-02` | `resolved` | first reader-shelf implementation still inferred workspace mode from scattered booleans and ad hoc null checks | the UI now had a real shelf, but package extraction toward `@portfolio/repub-builder` would stay brittle if `library`, `built-in reading`, and `local reading` were not modeled explicitly | extract a discriminated reader-workspace state contract in app code, add unit coverage for the mode resolver, and treat runtime extraction as the next follow-on instead of continuing with implicit state |
| `books-ai-01-attempt-01` | `resolved` | first Payload-backed admin ingest route implementation | importing Payload's SQLite adapter directly into the Next route bundle pulled `libsql` package docs into webpack and broke production builds | keep the canonical ingest path script-first for now, leave the route auth-gated but disabled, and defer the live admin trigger until the Next/Payload route integration is hardened |
| `books-ai-01-attempt-02` | `resolved` | first pass stored `vec0` tables in the same SQLite file as Payload | Payload/Drizzle schema introspection hit `no such module: vec0` because the Payload connection does not load the `sqlite-vec` extension before inspecting virtual tables | move FTS + `sqlite-vec` into a sidecar RAG SQLite file, keep Payload's canonical source/chunk/run records in its own SQLite file, and clean any stray vec tables out of the Payload DB before ingest |
| `books-ai-01-attempt-03` | `resolved` | local ingest verification on Windows dev machine | the original `better-sqlite3` search layer is blocked by ignored native build scripts and file-lock rebuild failures in this workspace | switch the search index connection to `libsql`, keep the ingest runner self-bundled instead of `tsx`, and validate the first full ingest against the sidecar RAG DB |
| `books-ai-01-attempt-04` | `resolved` | first `libsql` + `sqlite-vec` sidecar writes | `vec0` primary-key inserts failed even with numeric JS values because the `libsql` binding only satisfies the extension when the PK is bound as a `BigInt` | keep the sidecar DB on `libsql`, but bind `chunk_id` as `BigInt` when inserting vector rows |
| `books-ai-01-attempt-05` | `resolved` | first successful ingest commit pass | bulk `payload.update()` calls to toggle `rag-chunks.isActive` revalidated relationship fields and failed on SQLite round-tripped relationship ids like `30.0` | activate/deactivate chunk rows with direct SQL updates in the Payload SQLite file instead of per-row Payload updates |
| `books-ai-01-attempt-06` | `resolved` | first chat-side citation wiring pass | relying on client-only CopilotKit instruction hooks risked missing the active turn because retrieval finished asynchronously before the agent run started | enrich `agent/run` requests server-side with retrieved RAG context, then keep citation rendering in the client so the answer grounding and the visible Sources panel stay aligned |
| `books-ai-01-attempt-07` | `resolved` | assistant-ui shell migration | `useLocalRuntime` in the installed assistant-ui stack accepts the chat-model adapter directly, not the nested local-core `{ adapters: { chatModel } }` shape, and the first migration pass also needed DOM test shims for `ResizeObserver` / `scrollTo` | pass the adapter directly into `useLocalRuntime`, keep the backend on a thin `/api/chat` route, and harden unit tests with minimal DOM shims instead of backing away from the migration |
| `books-ai-01-attempt-08` | `resolved` | post-migration cleanup | leaving a deprecated `/api/copilotkit` compatibility route kept two public chat paths in the repo even after the assistant-ui cutover | remove the route, drop its tests, and treat assistant-ui + `/api/chat` as the only supported public chat stack |
| `books-ai-01-attempt-09` | `resolved` | first assistant-ui `/api/chat` debug pass after hard cutover | the route could fail as a black box during upstream 502s because there was no request id, retrieval timing, upstream status/body preview, or client-side send logging, while RAG ingest still used one-off console logs | add a shared env-driven structured logger, wire it through the client chat flow and `/api/chat`, keep a small in-memory recent-log buffer for inspection, and route existing ingest logs through the same logger |
| `books-ai-01-attempt-10` | `active` | live assistant-ui chat after replacing the API key | the running Next app still logged `429 insufficient_quota` for both embeddings and chat completions (`requestId` `9398561e-9156-4b47-b8f5-91f45b58c20a`) even after the key was updated, while isolated direct requests with the same `.env` succeeded outside the app path | treat this as a runtime env/process mismatch or request-context issue until disproven; keep the exact logs in planning, do not mark the quota issue resolved, and investigate the live server process boundary before changing retrieval or UI behavior |
| `books-reader-05-attempt-01` | `resolved` | first server-backed reader sync route pass | importing Payload directly into a Next route hit the same libsql bundle boundary as the auth routes and broke production build | keep reader sync behind a worker boundary (`reader-state-worker`) so built routes stay clean while entitled sessions still get DB-backed reading state |
| `books-reader-05-attempt-02` | `resolved` | first backend-backed shelf pass after upload/settings writes landed | the workspace could save settings and upload EPUBs, but saved backend records were still invisible in the shelf and the continue-reading setting did not affect the opening posture | treat uploaded records as first-class shelf entries with a dedicated route query, a stable reader storage key, and a continue-reading auto-selection pass so the backend model changes actual workspace behavior |
| `books-reader-02-attempt-01` | `resolved` | first build-time annotation embedding pass | the artifact hash stored inside `META-INF/portfolio-annotations.json` cannot safely be a literal hash of the final annotated zip because the payload changes the zip bytes | treat the content hash as the canonical digest of the artifact with `portfolio-annotations.json` stripped, then use that same rule in both builder and reader runtime |
| `books-cross-title-attempt-01` | `resolved` | Magicborn-line cross-title beats | a one-off `tri-book-chronology-and-chapter-beats.mdx` page drifted from the section planning artifact set | fold beats and TB-* questions into [section State](/docs/books/planning/state#magicborn-line-world-order-and-chapter-beats); track work under **`books-cross-title-01`** in [task registry](/docs/books/planning/task-registry); document boundary in [requirements](/docs/books/requirements) |

## Follow-up Rule

| Field | Value |
| --- | --- |
| `new-record-trigger` | add a row when a books problem changes approach, not for every small note |
| `close-condition` | move a record to `resolved` only after the fix is shipped or the path is abandoned |
