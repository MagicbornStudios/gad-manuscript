---
title: Task Registry
description: Compact task records for the books section.
date: "2026-03-15"
updated: "2026-04-06"
repoPath: apps/portfolio/content/docs/books/planning/task-registry.mdx
order: 3
---

# Task Registry

## Id shape (books section)

All ids here use **`<books>-<stream>-<phase>-<task>`** (see [Global planning guide](/docs/global/global-planning)). Streams in this registry:

| Stream | Meaning |
| --- | --- |
| `reader` | EPUB reading surfaces, annotations, read-page planning strip |
| `publishing` | Editions and which book EPUBs are live |
| `ai` | Site chat + RAG roadmap: **`books-ai-00`** = shipped CopilotKit (task `books-ai-00-01`); **`books-ai-01`** = forward Payload / `sqlite-vec` / ingest / citations queue |
| `cross-title` | Magicborn-line fiction across *Tale* / *Rune Path* / *Legacy*: world order + chapter beats + TB-* questions in [section State](/docs/books/planning/state#magicborn-line-world-order-and-chapter-beats) |

Legacy-looking ids such as `books-ai-01-04` are **not** a separate scheme: **`ai`** is the stream name, same as **`reader`** in `books-reader-01-16`.

## Phase `books-reader-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-reader-01-01` | `done` | move section planning docs into the books docs area | `-` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-02` | `done` | improve the dedicated reader so it feels like the primary reading surface | `books-reader-01-01` | `pnpm run build` |
| `books-reader-01-03` | `done` | make the homepage CTA mount the featured book reader and expose a disabled coming-soon second book | `books-reader-01-02` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-04` | `done` | fix EPUB image packaging so the front-page reader stops resolving broken archive assets | `books-reader-01-03` | `pnpm run build:books`, `pnpm run build`, `pnpm run lint` |
| `books-reader-01-05` | `done` | make the book build pipeline treat empty upcoming-book folders as `coming soon` instead of logging a failed EPUB build | `books-reader-01-04` | `pnpm run build:books` |
| `books-reader-01-06` | `done` | change the front-page reader to load EPUB bytes before mounting `react-reader` so browser requests stop falling back to `/META-INF/container.xml` | `books-reader-01-05` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-07` | `done` | make the homepage act as a reader launch surface that routes into `/apps/reader*book=<slug>` instead of depending on a second embedded runtime | `books-reader-01-06` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-08` | `done` | add header-level book switching on the dedicated reader page so the read route can move between available books without returning to the index | `books-reader-01-07` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-09` | `done` | tune the dedicated reader for wide-screen spreads and a richer TOC presentation | `books-reader-01-08` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-10` | `done` | shift the dedicated reader spread farther off the TOC toggle and align the reading stage with the header chrome | `books-reader-01-09` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-11` | `done` | rewrite emitted EPUB page shells so lead images sit under the page title, opening paragraphs stay visible, and folio numbers render at the bottom | `books-reader-01-10` | `pnpm run build:books`, `pnpm run build` |
| `books-reader-01-12` | `done` | add a subtle bottom navigation strip with current section title, previous/next controls, and direct page jumps on the dedicated reader | `books-reader-01-11` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-13` | `done` | define a formal manuscript convention for acts, chapters, chapter openers, and story pages, then teach the EPUB builder to emit that structure | `books-reader-01-12` | `pnpm run build:books`, `pnpm run build` |
| `books-reader-01-14` | `done` | fix EPUB spine generation so story pages excluded from the TOC still appear in reading order | `books-reader-01-13` | `pnpm run build:books`, `pnpm run build` |
| `books-reader-01-15` | `done` | replace the built-in TOC with a truly collapsed custom panel, restore wide-screen two-page spreads, and keep every illustrated page on the same fixed image frame shape as the placeholder state | `books-reader-01-14` | `pnpm run build:books`, `pnpm run build` |
| `books-reader-01-16` | `done` | ship reader annotations (highlights/comments, CFI, IndexedDB, list/jump/delete/edit note) | `books-reader-01-15` | `pnpm run build`, `pnpm run lint` |
| `books-reader-01-17` | `done` | JSON export/import for annotation sets + optional annotated EPUB zip with `META-INF/portfolio-annotations.json` | `books-reader-01-16` | manual reader smoke test |
| `books-reader-01-18` | `done` | read-only planning/docs panel on dedicated reader route for Mordred's Tale | `books-reader-01-15` | `pnpm run build` |

## Phase `books-publishing-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-publishing-01-01` | `planned` | expand planning records around publishing, editions, and progression | `books-reader-01-15` | `pnpm run build:books` |
| `books-publishing-01-02` | `planned` | build a live EPUB for the next visible book once the manuscript is ready | `books-publishing-01-01` | `pnpm run build:books`, `pnpm run build` |

## Phase `books-publishing-03` (Magicborn media + prompts)

**Goal:** **Cover and scene imagery** for Magicborn-line books using a **shared design-style prompt** plus **curated scene prompts** from chapter beats — same composition for **SiteCopilot**, **`POST /api/media/generate`**, and a **local CLI**. **Kickoff:** [plans/books-publishing-03-magicborn-media/PLAN.mdx](/docs/books/planning/plans/books-publishing-03-magicborn-media/plan). **Auth:** generation and owner editing paths are **owner/admin-only**; see **`global-auth-03`**.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-publishing-03-01` | `done` | add an in-repo **prompt library**: Magicborn **style block** (`lib/magicborn-prompts/style-block.ts`) + **scene seeds** (`scene-seeds.ts`); expand over time from beats | `-` | `pnpm --dir apps/portfolio exec vitest run tests/unit/lib/magicborn-compose-image-prompt.test.ts`; `pnpm run build` |
| `books-publishing-03-02` | `done` | **`composeMagicbornImagePrompt`** in `lib/magicborn-prompts/compose-image-prompt.ts`; **`POST /api/media/generate`** supports **`useMagicbornStyle`** + **`sceneKey`**; SiteCopilot sends **`useMagicbornStyle: true`** for app covers | `books-publishing-03-01` | unit + media route tests; `pnpm run build` |
| `books-publishing-03-03` | `planned` | **CLI** to generate images locally (batch or single) using the composer + `OPENAI_API_KEY`; write artifacts to an agreed path (`public/` / `media/` / book folder); document in kickoff | `books-publishing-03-02`, `global-auth-03-01` (CLI may use server key via env, not session) | script run documented; `pnpm run build` |
| `books-publishing-03-04` | `planned` | wire **cover** outputs into book **manifest / featured-book / OG** metadata paths so shipped surfaces pick up generated art | `books-publishing-03-03` or manual asset drop | `pnpm run build:books` where applicable; featured book shows cover |
| `books-publishing-03-05` | `planned` | **Scenes + EPUB gaps:** Payload **series / book / scene** + **media variants** (image, **scene play**, video prompt); **fast scan** of built EPUBs for **illustration pending** with **reading-order context** for compose; ties into Copilot batch + **`composeMagicbornImagePrompt`** — [global kickoff](/docs/global/planning/plans/magicborn-books-scenes-epub/KICKOFF), **`global-tooling-06`** | `books-publishing-03-02`, `global-tooling-06-02` | scan script tests + one book fixture |

## Phase `books-publishing-02` (Mordred's Tale trilogy packaging)

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-publishing-02-01` | `done` | split Tale packaging inputs into trilogy slugs (`mordreds_tale_part_1`, `_part_2`, `_part_3`) with chapter-range folders and book metadata | `-` | folder diff under `books/`, `pnpm run build:books` |
| `books-publishing-02-02` | `done` | align section planning state to trilogy reading order and slugs | `books-publishing-02-01` | [books state](/docs/books/planning/state#world-chronology-and-reading-order) |
| `books-publishing-02-03` | `planned` | expand Part III manuscript scope beyond Ch15 scaffold so `boughfall` is a full book and not only a seed EPUB | `books-publishing-02-01` | chapter plan + `pnpm run build:books` |

## Phase `books-ai-00`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-ai-00-01` | `done` | AI-v0: site floating AI chat (CopilotKit + OpenAI mini); document env in `.env.example` | `-` | `pnpm run build`, `pnpm run lint` |

## Phase `books-reader-02`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-reader-02-01` | `done` | P4: merge exported annotations into `build:books` output for fixed **repub reading artifacts** (same emitted **`book.epub`** / future bundle per [decisions](/docs/books/planning/decisions) **`BOOKS-REPUB-ARTIFACT`**) | `books-reader-01-17` | `pnpm --filter @portfolio/repub-builder test`, `pnpm run build:books` |
| `books-reader-02-02` | `done` | bundle planning trees into the EPUB spine: `repub epub ... --planning <dir>` (repeatable) plus per-book `book.json` `epubPlanningDirs` (paths relative to the book folder); walks `.md`/`.mdx`/`.xml`/`.toml`/`.txt` (skips `node_modules`, `.git`, `dist`, `.next`); appendix items are excluded from the generated TOC | `-` | `pnpm run build:books` |

## Phase `books-reader-04` (repub artifact + book-scoped cockpit packs)

**Goal:** **Reader workspace** and **Repo Planner cockpit** treat each **repo-built title** as a **repub reading artifact**: EPUB3 **plus** planning supplement already embedded at build time (**`books-reader-02-02`**), later **annotations** (**`books-reader-02-01`**). **No** reliance on **`/docs/...`** for title planning inside the reader; site **`builtin-packs.json`** stays **monorepo-wide** only. See [embed architecture](/docs/documentation/reader-repo-planner-embed-architecture) and **`BOOKS-READER-PLANNING-PACK-FROM-BOOK-ARTIFACT`**.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-reader-04-01` | `done` | **Spec** optional **`META-INF/`** manifest listing embedded planning appendix paths + version; align with **`PlanningPack`** / embed JSON shape for cockpit injection | `books-reader-02-02`, `books-reader-03-06` | [Spec](/docs/books/planning/plans/books-reader-04-epub-planning-pack); emitter in **`repub epub`**; `pnpm run build` |
| `books-reader-04-02` | `done` | **Runtime:** when **`readingTargetId`** is active, derive **per-book `builtinPacks`** from the **loaded EPUB** (zip + manifest / fallback `plan-*.xhtml`); JSON **`book-packs/<slug>.json`** as fallback only | `books-reader-04-01`, `repo-planner-integration-04-02` | manual: open cockpit from strip shows book planning; `pnpm run build`; `pnpm --filter @portfolio/repub-builder test` |

## Phase `books-reader-05` (backend-backed workspace + gated editing)

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-reader-05-01` | `done` | define and implement the auth-aware reader foundation: backend library records, explicit upload boundaries for EPUB persistence, owner entitlements, reader settings contract, and anonymous fallback rules without changing the public reading baseline | `global-auth-01-01`, `books-reader-03-10` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `books-reader-05-02` | `done` | persist annotations and reading progress to the database for authenticated entitled sessions while keeping IndexedDB/local export fallback for anonymous use | `books-reader-05-01`, `books-reader-01-17` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `books-reader-05-03` | `done` | add gated in-reader editing and write-back paths for owner sessions only | `books-reader-05-01`, `books-reader-05-02` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `books-reader-05-04` | `done` | polish the reader workspace UI around the backend-backed model: better shelf details, search, actions, and auth/settings affordances | `books-reader-05-01` | `pnpm run build`, `pnpm run lint` |

## Phase `books-reader-06` (shell cleanup + package-local shadcn parity)

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-reader-06-01` | `done` | restore the shipped shell contract: remove the extra reader hamburger/mobile drawer, keep only the portfolio sidebar plus one reader workspace rail, and render the built-in/queued shelf sections again so repo books remain first-class in the library | `books-reader-05-04` | `pnpm --dir apps/portfolio exec vitest run tests/unit/components/books/ReaderWorkspace.test.tsx`, `pnpm run build`, `pnpm run lint` |
| `books-reader-06-02` | `done` | give `@portfolio/repub-builder` package-local shared UI primitives that mirror the portfolio shadcn/base-ui tokens and migrate the reader workspace, shelf cards, and shell controls onto them where practical | `books-reader-06-01` | `pnpm --dir apps/portfolio exec vitest run tests/unit/components/books/ReaderWorkspace.test.tsx`, `pnpm run build`, `pnpm run lint` |
| `books-reader-06-03` | `done` | lock the cleanup pass with targeted reader component coverage and any required docs/decision updates so the reader returns to maintenance mode on a stable shell contract | `books-reader-06-01`, `books-reader-06-02` | `pnpm --dir apps/portfolio exec vitest run tests/unit/components/books/ReaderWorkspace.test.tsx`, `pnpm run build`, `pnpm run lint` |

## Phase `books-reader-07` (shadcn control migration)

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-reader-07-01` | `done` | add the next mirrored package-local reader UI primitives (`Textarea`, `Select`, `Checkbox`) and migrate workspace settings/upload/forms plus planning-strip and modal buttons onto them | `books-reader-06-02` | `pnpm --dir apps/portfolio exec vitest run tests/unit/components/books/ReaderWorkspace.test.tsx`, `pnpm run build`, `pnpm run lint` |
| `books-reader-07-02` | `done` | migrate the main `EpubViewer` **chrome only** (toolbars, overlays, annotation UI, page-jump): swap raw controls for package-local primitives (`@portfolio/repub-builder` reader UI). **Do not** replace the **`react-reader` / EPUB reading stage** or bespoke spread/CFI layout — that stays the existing viewer core | `books-reader-07-01` | `pnpm --filter @portfolio/repub-builder test`, `pnpm run build`, `pnpm run lint` |
| `books-reader-07-03` | `done` | record the remaining intentionally custom reader surfaces and add targeted regression coverage for the migrated controls | `books-reader-07-01`, `books-reader-07-02` | `reader-ui-primitives.test.ts` + existing repub-builder tests, `pnpm --dir apps/portfolio exec vitest run tests/unit/components/books/ReaderWorkspace.test.tsx`, `pnpm run build`, `pnpm run lint` |

### Definition of done (books-reader-07)

| Layer | Done when |
| --- | --- |
| **Reading stage** | Still driven by **`EpubViewer`** + **`react-reader`** (same underlying reader as today); no requirement to wrap the iframe/canvas in generic shadcn layout primitives. |
| **Chrome / controls** | Toolbars, dialogs, annotation panel, page jump, and other **surrounding** UI use **package-local** mirrored shadcn-style components where a straight swap does not break layout or a11y. |
| **`07-01`** | **Done** — `Textarea` / `Select` / `Checkbox` + workspace/settings/planning-strip migrations per task row. |
| **`07-02`** | **Done** — `EpubViewer` + `ReaderModalRoot` interactive surfaces use `Button` / `Input` / `Textarea`; **exceptions (leave as-is):** hidden file input for JSON import; **`motion.button`** TOC/notes panel scrims (Framer opacity + layout; not worth wrapping with `motion(Button)` without a refactor). |
| **`07-03`** | **Done** — remainder documented below; **`reader-ui-primitives.test.ts`** locks the chrome primitive exports; full DOM coverage deferred as non-blocking. |

### Remainder (books-reader-07-03)

Phase **closed.** Intentionally custom surfaces:

- **`motion.button`** full-screen backdrop dismiss for TOC and notes panels — animation boundary; keep native `motion.button`.
- **Hidden `<input type="file">`** for annotations import — standard file picker pattern.

## Phase `books-reader-03` (reader shell + library UX)

**Goal:** One **reader app shell** with an **overview / library** step (book grid or list, "open local EPUB", clear navigation between shelf and reading). **`/books` HTML routes are removed** (308 -> `/apps/reader`); catalog UX lives in the shell. **Implementation home:** **`ReaderWorkspace`**, **`EpubViewer`** (lazy boundary), and **most reader/workspace/shelf chrome** move into **`@portfolio/repub-builder`** in the **same extraction track** and are imported by **`apps/portfolio`** only for **v1** (**`BOOKS-READER-V1-PORTFOLIO-HOST-ONLY`**). **`/apps/reader`** stays a thin route that passes manifest, links, and env. **UX target (locked):** **shadcn dashboard** -- **sidebar** = Library / current book / Settings (**no** team switcher); **SidebarInset** = **searchable EPUB catalog** (Unity **Asset Store-style** density: search, filters, scannable grid) **or** **`EpubViewer`**; **toolbar** = breadcrumb/title, planning-strip toggle, theme, download when relevant, **EPUB upload** (picker + **drag-and-drop**). **Theme:** bundled in package, **visually matches** portfolio reader palette (**`BOOKS-READER-THEME-BUNDLED-PARITY`**). **URLs:** **query params** for v1 (**`BOOKS-READER-QUERY-URL-V1`**). **Package boundary:** stable **runtime export** from `@portfolio/repub-builder` (see [Route conventions](/docs/documentation/route-conventions) and **`BOOKS-READER-RUNTIME-REPUB-PACKAGE`**). **Kookit** (`vendor/kookit`): **navigation / reader-chrome inspiration** for the shell, not something we dismiss as "only utils."

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-reader-03-01` | `done` | RFC or short ADR: **`ReaderWorkspace` + `EpubViewer` move together**; **`ReaderShelfCard`** and related shelf primitives; **public props + callbacks** for the portfolio host; **dashboard regions** per **`BOOKS-READER-DASHBOARD-CHROME`**; **shadcn peer vs bundled** note (**`BOOKS-READER-SHADCN-IN-REPUB-BUILDER`**); **export map** subpath | `-` | ADR table on [Route conventions](/docs/documentation/route-conventions); `pnpm run build`; `pnpm --filter @portfolio/app test:unit` |
| `books-reader-03-02` | `done` | implement **library overview** inside `/apps/reader` when no `book` query (or explicit `*view=library`): cards for `getBooks()`, CTA for local EPUB, matches Kookit-inspired clarity | `books-reader-03-01` | manual UX pass; `pnpm run build` |
| `books-reader-03-03` | `done` | **Remove `/books` app routes**; **`next.config` redirects** to `/apps/reader`; keep static **`/books/<slug>/book.epub`** under `public/books/` | `-` | `pnpm run build`; hit `/books` -> reader |
| `books-reader-03-04` | `done` | **Shipped:** runtime **`@portfolio/repub-builder/reader`**, shelf search, **DnD + picker**, **site `SidebarTrigger`** in toolbar (no package duplicate sidebar), Asset Store-style **catalog filters** (All / Ready / Coming soon / Reading / New / Done), toolbar **ember vs ink** theme (localStorage), **`ReaderShelfCard`** respects chrome theme | `books-reader-03-01`, `books-reader-03-02` | `pnpm run build`; `pnpm --filter @portfolio/app test:unit` |
| `books-reader-03-05` | `done` | align **Nav** "Books" / "Apps" active states (Apps not active on **`/apps/reader`**) and **homepage** "Browse library" + reader CTAs | `books-reader-03-03`, `documentation-site-06-02` | smoke on nav + home; `pnpm run build` |
| `books-reader-03-06` | `done` | **`readerModalStore`** + **`ReaderModalRoot`** landed in reader shell for optional host modals. *Superseded (2026-04):* portfolio does **not** pass **`renderPlanningCockpit`** / **`RepoPlannerCockpitClient`**; planning is **GAD + Planning pack** downloads, not an in-reader cockpit. | `books-reader-03-01` | `pnpm run build`; optional **`renderReaderModal`** for other hosts |
| `books-reader-03-07` | `done` | **`ReaderPlanningStrip`** in package; **`openPlanningCockpit`** on **`readerModalStore`** only (**no** global **`useModalStore`** on strip); portfolio **`ReaderPlanningStrip.tsx`** removed | `books-reader-03-06` | manual reader smoke |
| `books-reader-03-08` | `done` | extend the generated books manifest and `BookEntry` model with library-facing metadata: cover asset or fallback key, author, description, and status hints so `/apps/reader` can render a real built-in shelf without EPUB introspection at runtime | `books-reader-03-01` | `pnpm run build:books`, `pnpm run build` |
| `books-reader-03-09` | `done` | redesign the `/apps/reader` default surface as a Koodo-inspired built-in shelf: always-visible repo books first, prominent covers, EmptyCover fallback art, progress-first status chips (`New`, `%`, `Done`, `Coming soon`), and local EPUB import as a secondary utility card/action | `books-reader-03-02`, `books-reader-03-08` | manual UX pass, `pnpm run build` |
| `books-reader-03-10` | `done` | formalize the reader workspace state model for library, built-in reading, and local-file reading so the eventual `@portfolio/repub-builder` runtime export has a clean public API | `books-reader-03-04`, `books-reader-03-09` | `pnpm --filter @portfolio/app test:unit`, `pnpm run lint`; full `pnpm run build` currently blocked by unrelated repo-planner module-resolution errors |

## Phase `books-ai-01` (forward queue)

Work after `books-ai-00` and alongside `books-reader-02` / `books-publishing-01`. Monorepo scope + AI notes: [documentation/requirements](/docs/documentation/requirements).

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-ai-01-01` | `done` | AI-v1a: define Payload CMS collections (`sources`, `chunks`, `ingest-runs`, optional messages), source-of-truth boundaries, and SQLite ownership for chat retrieval | `books-ai-00-01` | `pnpm run build`, `pnpm run lint` |
| `books-ai-01-02` | `done` | AI-v1b: plan `sqlite-vec` loading strategy, `vec0` table shape, metadata/aux column usage, and `text-embedding-3-small` `1536d` cosine storage | `books-ai-01-01` | `pnpm run build`, `pnpm run lint` |
| `books-ai-01-03` | `done` | AI-v1c: design ingest + chunking pipeline for `content/docs`, `content/projects`, `content/blog`, and `magicborn` world-building material; exclude raw manuscript text by default; define reindex/checksum rules | `books-ai-01-02` | `pnpm run build`, `pnpm run lint` |
| `books-ai-01-04` | `done` | AI-v1c: design retrieval contract for query embeddings, vector top-k search, lightweight rerank/filter, and citation payloads back to chat | `books-ai-01-03` | `pnpm run build`, `pnpm run lint` |
| `books-ai-01-05` | `done` | AI-v1c: implement chat UX to render cited chunks inside a collapsible Sources panel with source links and snippets | `books-ai-01-04` | `pnpm test:unit`, `pnpm test:e2e`, `pnpm run build`, `pnpm run lint` |
| `books-ai-01-06` | `done` | AI-v1d: basic-auth protection for Payload admin and ingest/reindex controls, plus script-first ingest and a deferred admin-trigger posture | `books-ai-01-01` | `pnpm run build`, `pnpm run lint`, `pnpm --filter @portfolio/app rag:ingest` |
| `books-ai-01-07` | `done` | migrate the public site chat shell from CopilotKit to assistant-ui with a Claude-style modal UI and mounted devtools, while keeping the existing RAG backend and citation model | `books-ai-01-05` | `pnpm test:unit`, `pnpm test:e2e`, `pnpm run build`, `pnpm run lint` |
| `books-ai-01-08` | `done` | add unit and e2e coverage for the public chat shell, RAG retrieval routes, and admin auth boundary | `books-ai-01-05`, `books-ai-01-06` | unit tests, e2e tests, `pnpm run build`, `pnpm run lint` |
| `books-ai-01-09` | `done` | add scoped structured logging for the assistant-ui chat path and RAG ingest flow so request, retrieval, upstream, and error handling are easy to inspect without permanent ad hoc debug logs | `books-ai-01-07`, `books-ai-01-08` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `books-ai-01-10` | `planned` | investigate the live runtime mismatch where `/api/chat` still returns OpenAI `429 insufficient_quota` after an API-key update, even though isolated direct requests with the same `.env` succeed | `books-ai-01-09` | reproduce in live dev server, inspect env/process boundary, confirm with targeted smoke logs |

## Phase `books-cross-title-01` (Magicborn line)

**Goal:** Keep cross-title fiction planning in **section** `state` / `task-registry` / `decisions` — no parallel `planning/*.mdx` pages outside the standard set.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `books-cross-title-01-01` | `done` | World order + per-book chapter beat tables + TB-* questions live under [State — Magicborn line](/docs/books/planning/state#magicborn-line-world-order-and-chapter-beats); per-title deep sheets stay on stream **state** pages | `-` | planning IA review |
| `books-cross-title-01-02` | `planned` | Close TB-* questions → tighten State tables → sync stream state pages and manuscript | `books-cross-title-01-01` | fiction session notes + State diff |

## Registry Rules

| Field | Value |
| --- | --- |
| `ordering` | rows stay in intended execution order |
| `status-set` | `planned`, `in-progress`, `done`, `blocked` |
| `verification` | list the smallest command set that proves the task |
